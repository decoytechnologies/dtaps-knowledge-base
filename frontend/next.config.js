/** @type {import('next').NextConfig} */
const nextConfig = {
  // We leave this empty for now to ensure a successful build.
  // The core issue is the file extension and syntax.
};

module.exports = nextConfig;
